header.header
  .header__column
    a(href=routes.home)
      span.header__home-link
        i.fab.fa-youtube 
        span Premium
  .header__column
    form(action=routes.search, method="get")
      input.header__search-box(type="text", name="search_query", placeholder="검색")
      input.header__search-submit(type="submit", value="검색")
  .header__column
    if user
      ul
        li
          a(href=`/videos${routes.upload}`) Upload
        li
          a(href=routes.profile) Profile
        li
          a(href=routes.logout) Logout
    else
      ul
        li
          a(href=routes.join) Join
        li
          a(href=routes.login) Login
      