extends layouts/main
include mixins/videoBlock

block content
  if(videos)
      .main-content
        each video in videos
          +videoBlock({
            id:video.id,
            title:video.title,
            src:video.src
          })